resource:
  id: "{{/name}}"
  alias: "{{/name}}"
  requiredAttributes:
    - /name
operations:
  list:
    path: /services/haproxy/sites
  get:
    path: /services/haproxy/sites/{{/id}}
  create:
    method: POST
    path: /services/haproxy/sites
    validate:
      requiredAttributes:
        - /name
      schemaRef: openapi:request-body
  update:
    method: PUT
    path: /services/haproxy/sites/{{/id}}
    validate:
      requiredAttributes:
        - /name
      schemaRef: openapi:request-body
  delete:
    path: /services/haproxy/sites/{{/id}}
  compare:
    path: /services/haproxy/sites/{{/id}}
