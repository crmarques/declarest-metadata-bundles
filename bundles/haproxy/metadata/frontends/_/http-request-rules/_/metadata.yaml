resource:
  id: "{{/index}}"
  alias: "{{/index}}"
  remoteCollectionPath: /services/haproxy/configuration/frontends/{{/frontend}}/http_request_rules
  requiredAttributes:
    - /index
    - /type
operations:
  list:
    path: .
    transforms:
      - jqExpression: 'to_entries | map(.value + {index: (.key | tostring)})'
  get:
    path: ./{{/id}}
    transforms:
      - jqExpression: '. + {index: "{{/id}}"}'
  create:
    method: POST
    path: ./{{/index}}
    transforms:
      - excludeAttributes:
          - /index
    validate:
      requiredAttributes:
        - /type
      schemaRef: openapi:request-body
  update:
    method: PUT
    path: ./{{/id}}
    transforms:
      - excludeAttributes:
          - /index
    validate:
      requiredAttributes:
        - /type
      schemaRef: openapi:request-body
  delete:
    path: ./{{/id}}
  compare:
    path: ./{{/id}}
    transforms:
      - jqExpression: '. + {index: "{{/id}}"}'
