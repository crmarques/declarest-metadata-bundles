# CLAUDE

Claude Code agents MUST follow `AGENTS.md` in this repository.

Start by reading `AGENTS.md`, then load the local docs and upstream
`../declarest` references selected by its routing table. Keep `AGENTS.md` as
the canonical instruction source so Claude-specific guidance does not drift
from the shared agent workflow.
